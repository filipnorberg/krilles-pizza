<div class="menus">
  <div class="menu">
    <h2 class="rufina">Pizza</h2>
    <p class="merriweather">*Alla pizzor har tomatsås, ost och surdegsbotten*</p>
    {% if pizza.items %} {% for item in pizza.items %}
      <div class="item nunito">
        <div class="upper">
          <h3>{{ item.name }}</h3>
          <h3 class="right-inner">{{ item.price }}kr</h3>
          <img class="right svg" src="/assets/svg/AddShoppingCart.svg" />
      </div>
      <p class="flex nunito">{{ item.description }}</p>
    </div>
    {% endfor %} {% endif %}
  </div>
  <br />
  <div class="menu">
    <h2 class="rufina">Grill</h2>
    {% if grill.items %} {% for item in grill.items %}
      <div class="item nunito">
        <div class="upper">
          <h3>{{ item.name }}</h3>
          <h3 class="right-inner">{{ item.price }}kr</h3>
          <img class="right svg" src="/assets/svg/AddShoppingCart.svg" />
      </div>
      <p class="flex nunito">{{ item.description }}</p>
    </div>
    {% endfor %} {% endif %}
  </div>
  <br />
  <div class="menu">
    <h2 class="rufina">Vegetariskt</h2>
    {% if vego.items %} {% for item in vego.items %}
      <div class="item nunito">
        <div class="upper">
          <h3>{{ item.name }}</h3>
          <h3 class="right-inner">{{ item.price }}kr</h3>
          <img class="right svg" src="/assets/svg/AddShoppingCart.svg" />
      </div>
      <p class="flex nunito">{{ item.description }}</p>
    </div>
    {% endfor %} {% endif %}
  </div>
</div>
